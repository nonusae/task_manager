
<%=form_tag multiple_delete_tasks_path, method: :delete do%>
<table class="table table-hover table-bordered" class="main_table">
	<thead>
	</thead>
	<tbody id= "tasks">		
		<%= render @tasks %> <%# this will make rails render partial wiht _post.html.erb name and rails will auromatic do the |post| %>
</tr>
	</tbody>
</table>

<a class= "col-md-offset-10 col-md-3" id="delete-tasks-button" style="display:none"><%= button_tag  "Delete selected" %></a>
</div>
<% end %>